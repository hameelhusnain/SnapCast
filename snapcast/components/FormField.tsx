import { Placeholder } from "drizzle-orm"

const FormField = ({ id, label, type-'text', value, onChange, Placeholder, as = 'input'}) => {
  return (
    <div>FormField</div>
  )
}
 
export default FormField